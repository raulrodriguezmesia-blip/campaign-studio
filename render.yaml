services:
  - type: web
    name: campaign-studio-api
    runtime: python
    # IMPORTANT: leave "Root Directory" EMPTY (repo root) so Render finds
    # requirements.txt and wsgi.py at the root. Do NOT set it to "backend".
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn wsgi:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: OPENAI_API_KEY
        sync: false
      - key: CORS_ORIGINS
        value: "https://campaign-studio-new.vercel.app,http://localhost:8080,http://localhost:3000"
